---
description: Execute a complete TDD cycle (RED → GREEN → REFACTOR)
---

NOTE: Consider using `/tdd cycle` instead - part of unified TDD workflow.

Execute the tdd-cycle skill to run through a complete TDD cycle (RED → GREEN → REFACTOR → COMMIT).
